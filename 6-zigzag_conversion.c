char* convert(char* s, int numRows) {
    // malloc the result-string on the same size as the original one
    int size = 1;
    while (s[size - 1])
        size++;
    char    *newS = malloc(size * sizeof(char));

    
    int i = 0;
    
}